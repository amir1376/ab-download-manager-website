## 如何制作应用便捷程序（自定义配置目录）

默认情况下，应用将配置文件存储在：

`~/.abdm`

您可以通过以下两种方式更改此设置：

### 方案 1：使用本地的 `.abdm` 文件夹

在应用的安装目录内创建一个名为 `.abdm` 的文件夹。

当该文件夹存在时，应用程序会自动将其用作配置目录，而非默认位置。

### 方案 2：使用 `.portable` 文件

在安装目录内创建一个名为 `.portable` 的文件。

在此文件中，写入您希望用作配置目录的文件夹的完整路径。

应用程序将读取该文件，并使用指定的路径来存储配置数据。
